## Minimal requirements used by Vercel for the API build.
## Keep this list small to avoid exceeding Vercel serverless function size limits.
fastapi
uvicorn
pydantic

# If you need the full set of developer dependencies (pandas, streamlit, ...),
# install them from `requirements-full.txt` instead:
# pip install -r requirements-full.txt
